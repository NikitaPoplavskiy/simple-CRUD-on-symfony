{{ form_start(form) }}
    {{ form_widget(form, { 'attr': {'class': 'text-white h-100 justify-content-center align-items-center'}}) }}
    <button class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
